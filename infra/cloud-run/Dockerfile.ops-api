ARG BASE_IMAGE=node:22-bullseye-slim
FROM ${BASE_IMAGE}

WORKDIR /app

COPY . .

RUN npm run build -w @blinds/integrations \
  && npm run build -w @blinds/ops-api

ENV NODE_ENV=production

EXPOSE 8080

CMD ["sh", "-c", "PORT=${PORT:-8080} npm run start -w @blinds/ops-api"]
